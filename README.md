# Image-Edge-Detection-using-Sobel-Operator-with-Gaussian-Preprocessing
 Image edge detection using the Sobel operator with Gaussian preprocessing reduces noise and improves edge accuracy. The image is smoothed, then gradients are computed in horizontal and vertical directions to detect edges, producing reliable results for computer vision tasks.
